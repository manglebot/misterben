// import { Route, Routes } from "react-router-dom";

// import workData from "../Data/workData";
// import playData from "../Data/workData";

// const portfolioRoutes = [
//   ...workData.map((item) => (
//     <Route key={item.path} path={`/work/${item.path}`} element={item.element} />
//   )),
//   ...playData.map((item) => (
//     <Route key={item.path} path={`/play/${item.path}`} element={item.element} />
//   )),
// ];

// export default portfolioRoutes;
